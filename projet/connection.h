#ifndef CONNECTION_H
#define CONNECTION_H

#include <QWidget>
/*#include <QtWidgets>
#include <iostream>

class connection : public QWidget
{
    Q_OBJECT
public:
    explicit connection(QWidget *parent = 0);
    QString getHost();
    QString getDb();
    QString getUser();

signals:

public slots:

private:
    const QString ĥostname;
    static const QString dbname;
    static const QString username;
};

#endif // CONNECTION_H
